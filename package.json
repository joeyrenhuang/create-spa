{
  "name": "web-cli",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "bin": {
    "web": "src/index.coffee"
  },
  "scripts": {
    "dev": "if [ -z $(which web) ]; then  npm link;  fi; echo 'Coffeescript required! If coffee command not avaible, You need run manually:\nPATH=$(npm bin):$PATH'",
    "build": "coffee -o lib/ -c src/",
    "test": "cd __test__ && rm -rf * && web init vue && web init react && web"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "coffeescript": "^2.6.1"
  },
  "dependencies": {
    "commander": "^8.3.0",
    "inquirer": "^8.2.0",
    "shelljs": "^0.8.4"
  }
}
